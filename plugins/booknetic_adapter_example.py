from typing import Any, Dict, List


def fetch() -> List[Dict[str, Any]]:
    """
    Ejemplo de interfaz para tu scraper existente.
    Debe devolver una lista de diccionarios con estructura libre; nosotros guardamos en raw.
    Campos que recomendamos (si los tienes) para facilidad de uso:
    - id (string estable si lo tienes; si no, lo generaremos hash)
    - customer_name, customer_email, service_name, starts_at, status
    """
    # Reemplaza con la importación real de tu scraper y retorna su data
    # from my_repo.scraper import run
    # return run()
    return []


