[build]
builder = "NIXPACKS"

[deploy]
startCommand = "uvicorn app.main:app --host 0.0.0.0 --port $PORT --workers 1"

# Opcional: variables por defecto (puedes definirlas en Railway UI)
[variables]
PYTHONUNBUFFERED = "1"
TZ = "UTC"


